# Core dependencies (installed with `pip install cellarc`)
numpy
matplotlib
tqdm

# Optional extras used by generation and solver tooling (`pip install cellarc[all]`)
jax
flax
cax
datasets
huggingface_hub
pyarrow
